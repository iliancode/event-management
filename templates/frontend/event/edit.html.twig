{% extends 'base.html.twig' %}

{% block title %}Modifier le event : {{ event.title }}{% endblock %}

{% block body %}
    <div class="container mt-3">

        <h1>Modifier le event : {{ event.title }}</h1>
        <section class="py-3">
            {% include('frontend/event/_form.html.twig') with {'button_label': 'Modifier'} %}
        </section>

        <section class="py-3">
            <div class="row">
                <div class="col">
                    <a class="btn btn-secondary" href="{{ path('app.events') }}">Retour à la liste</a>
                </div>
            </div>
        </section>
    </div>

{% endblock %}